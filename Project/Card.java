package Project;

public enum Card {
    CONNECT,USER_INFO,CHAT,ROOMS
}


//private ServerThread getClientById(long id){ return cleints.stream().filter(c -> c.getClientId() == id).toList().get(0);}

// private ServerThread getClientByName(String name){ return clients.stream().filter(c->c.getClientName().equalsIgnoreCase(name.trim(())).toList().get(0) }
    
//protected void mutedYouBro(ServerThread muter, String User){ 
    //ServerThread target=getCleintByName(user); 
    //taget.sendMessage(muter.getClientId(),"Things are cool");
//}